function printName(person:{first:string;last:string}):void{
    console.log(`${person.first} ${person.last}`)
}
printName({first:'thomi',last:"jenl"})


let coordinate:{
    x:number;
    y:number;
}={x:34,y:45}

